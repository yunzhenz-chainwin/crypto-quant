.venv/
__pycache__/
*.py[cod]

.env
.env.*
*.log

.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

*.egg-info/
build/
dist/

# 前端依賴與建置產物
frontend/node_modules/
frontend/dist/

# 動態產生的資料（頻繁變動，不追蹤）
data/raw/
data/clean/
data/*.db
data/*.db-wal
data/*.db-shm
data/backups/

# 後端執行期資料庫（app.db 等，執行時產生，勿提交）
backend/*.db
backend/*.db-wal
backend/*.db-shm

# 執行期／研究重跑報表（可由資料與程式重建，不追蹤）
# reports/*.md 可作為人工審閱文件追蹤；固定測試資料請放 tests/fixtures/。
reports/*.csv
reports/*.json
reports/*.png
reports/validation_*.txt
reports/crosscheck_*.txt

# Claude 工作階段記憶(非專案程式)
memory/

# 本機密鑰載入檔（後台帳密，勿提交）。.cmd = Windows，.sh = macOS/Linux
*.local.cmd
*.local.sh

# macOS 檔案總管產生的垃圾
.DS_Store

# 由腳本重生的 docx 中繼產物（md2docx.py / export_qa_docs.py 隨時可重生）；
# 只提交合併後的 crypto-quant_文件合集.docx 與手工來源 情緒詞庫範本.docx
docs/crypto-quant_專案說明.docx
docs/訊號增準計畫.docx
docs/ML訊號研究計畫.docx
docs/AI機器人固定問答範本.docx
docs/預測模型指標報告.docx
