# 開發／驗證用相依(正式執行 app 不需要)
#
# 前端指標正確性驗證 scripts/verify_frontend_indicators.py 需要獨立第三方庫
# 當作參考,與 frontend/src/lib/indicators.js 的計算逐點比對:
pandas_ta

# 產生 Word 文件(計畫書/詞庫範本 → docs/*.docx)
python-docx

# 測試框架 + FastAPI TestClient 做 API smoke test
# starlette TestClient 依賴 httpx；新版建議 httpx2（httpx 仍可用但會出 Deprecation 警告）
pytest
httpx2
